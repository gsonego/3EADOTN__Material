# Resource Dictionary

Every Azure resource created across the course materials, grouped by module. Populated as resources are actually created (see `materials/module-revision-prd.md`) — not speculative.

## Module 1

| Resource Name | Type | Created In (section/step) | Notes |
| -------------- | ---- | -------------------------- | ----- |
| `rg-estiam-dev-2` | Resource Group | Section 2 — Resource Groups | westcentralus. Recreated under the `-dev-2` suffix after the original `-dev-1` group was deleted at the end of an earlier authoring session; this table reflects the live `-dev-2` environment |
| `acrestiamdev2` | Container Registry (ACR) | Section 4 — Topic 1 App Service | Basic SKU, admin-enabled; holds both `catalog-ui` and `catalog-api` images |
| `asp-estiam-dev-2` | App Service Plan | Section 4 — Topic 1 App Service | Linux, B1 |
| `webapp-estiam-dev-2` | Web App (App Service) | Section 4 — Topic 1 App Service | Created with `acrestiamdev2.azurecr.io/catalog-ui:v1`; URL: `https://webapp-estiam-dev-2.azurewebsites.net`. **Image rolls forward across the course as the one `catalog-ui` project grows in place** — `v2` and `v3` in Module 4, `v4` in Module 6. Current: `catalog-ui:v4` |
| `env-estiam-dev-2` | Container Apps Environment | Section 5 — Topic 2 Container Apps | |
| `workspace-rgestiamdev2NzD5` | Log Analytics Workspace | Section 5 — Topic 2 Container Apps | Auto-generated by `containerapp env create` |
| `app-estiam-dev-2` | Container App | Section 5 — Topic 2 Container Apps | Target port 8080; FQDN: `app-estiam-dev-2.delightfulgrass-32a51ddf.westcentralus.azurecontainerapps.io`; multiple revision mode. Revisions: `app-estiam-dev-2--0000001` (image `catalog-api:v1`) and `app-estiam-dev-2--v2` (image `catalog-api:v2`, adds static `GET /titles`) — `v2` holds 100% traffic after the 5.2 demo |

## Module 2

| Resource Name | Type | Created In (section/step) | Notes |
| -------------- | ---- | -------------------------- | ----- |
| `cosmos-estiam-dev-2` | Cosmos DB Account | Section 1.2 — Topic 1 Cosmos DB | westcentralus, Session consistency, kind GlobalDocumentDB; in shared `rg-estiam-dev-2` |
| `CatalogDb` | Cosmos DB Database | Section 1.2 — Topic 1 Cosmos DB | Inside `cosmos-estiam-dev-2` |
| `Titles` | Cosmos DB Container | Section 1.2 — Topic 1 Cosmos DB | Partition key `/genre`; 400 RU/s manual throughput; database `CatalogDb` |
| `stestiamdev2` | Storage Account | Section 2.2 — Topic 2 Blob Storage | westcentralus, Standard_LRS; in shared `rg-estiam-dev-2` |
| `posters` | Blob Container | Section 2.2 — Topic 2 Blob Storage | In `stestiamdev2`; used both for the SAS mechanics demo and the Catalog app's real poster uploads (Section 2.4) |
| `catalog-api:v3` (image) | Container image / App revision | Section 1.3 — Topic 1 Cosmos DB | Pushed to `acrestiamdev2`; deployed to `app-estiam-dev-2` as revision `app-estiam-dev-2--0000003`. Real Cosmos DB-backed `/titles` CRUD, no poster support yet. Superseded by `v4` below |
| `catalog-api:v4` (image) | Container image / App revision | Section 2.4 — Topic 2 Blob Storage | Pushed to `acrestiamdev2`; deployed to `app-estiam-dev-2` as revision `app-estiam-dev-2--0000004`, 100% traffic (this app is in multiple-revision mode — traffic had to be moved explicitly, see commands.md). Adds `POST /titles/{id}/poster` (Blob Storage) on top of v3's Cosmos DB persistence; `posterUrl` is a fresh SAS URL generated on every read |

## Module 3

| Resource Name | Type | Created In (section/step) | Notes |
| -------------- | ---- | -------------------------- | ----- |
| `kv-estiam-dev-2` | Key Vault | Section 2.2 — Topic 2 Managed Identity | westcentralus, `--enable-rbac-authorization true`; in shared `rg-estiam-dev-2`. Holds secret `CosmosConnectionString` (the real `cosmos-estiam-dev-2` connection string) |
| Role: Key Vault Secrets Officer → signed-in user | RBAC role assignment | Section 2.2 | Scope: `kv-estiam-dev-2`. Needed to write the secret — control-plane Owner does not imply data-plane write access |
| `app-estiam-dev-2` — system-assigned Managed Identity | Managed Identity | Section 2.2 | principalId `9d59f994-a72b-4440-b57b-0fccf6b4dfe7`; enabled via `az containerapp identity assign --system-assigned` |
| Role: Key Vault Secrets User → `app-estiam-dev-2` identity | RBAC role assignment | Section 2.2 | Scope: `kv-estiam-dev-2`. Without this, `az containerapp secret set` with `keyvaultref:` fails outright (Container Apps validates Key Vault access at secret-creation time, unlike Function App's app settings which resolve lazily) |
| `catalog-api:v5` (image) | Container image / App revision | Section 2.2 — Topic 2 Managed Identity | Pushed to `acrestiamdev2`; deployed to `app-estiam-dev-2` as revision `app-estiam-dev-2--0000005` (superseded by v6 below, no longer receiving traffic). `CosmosDb__ConnectionString` env var read `secretref:cosmos-conn`, which Container Apps resolves via `keyvaultref:` + the app's own managed identity — no plain connection string in the app config. `BlobStorage__*` unchanged from v4 |
| Role: Cosmos DB Built-in Data Contributor → signed-in user | Cosmos DB data-plane role assignment | Section 2.3 — Topic 2 Managed Identity | Scope: `cosmos-estiam-dev-2` (whole account, `--scope "/"`). Assigned via `az cosmosdb sql role assignment create` (a separate command from generic `az role assignment create`) so `dotnet run` locally could authenticate as the developer via `DefaultAzureCredential` |
| Role: Cosmos DB Built-in Data Contributor → `app-estiam-dev-2` identity | Cosmos DB data-plane role assignment | Section 2.3 — Topic 2 Managed Identity | Scope: `cosmos-estiam-dev-2` (whole account, `--scope "/"`). Lets the Container App's managed identity read/write Cosmos DB directly, no connection string or Key Vault involved |
| `catalog-api:v6` (image) | Container image / App revision | Section 2.3 — Topic 2 Managed Identity | Pushed to `acrestiamdev2`; deployed to `app-estiam-dev-2`, superseded by revision `app-estiam-dev-2--acrmi` (Section 2.4, same image). Drops `CosmosDb__ConnectionString` entirely; adds `CosmosDb__AccountEndpoint` and authenticates via `DefaultAzureCredential` (the Container App's managed identity in Azure, the developer's `az login` locally). `BlobStorage__*` unchanged |
| Role: AcrPull → `app-estiam-dev-2` identity | RBAC role assignment | Section 2.4 — Topic 2 Managed Identity | Scope: `acrestiamdev2`. Lets the Container App pull images from the private ACR without admin-enabled username/password |
| `app-estiam-dev-2--acrmi` | Container App revision | Section 2.4 — Topic 2 Managed Identity | Same image as v6 (`catalog-api:v6`), redeployed with `--revision-suffix acrmi` specifically to force a fresh image pull under the new identity-based registry auth (`az containerapp registry set --identity system`). 100% traffic. Verified `Running` and serving real data with the ACR admin user fully disabled |
| `webapp-estiam-dev-2` — system-assigned Managed Identity | Managed Identity | Section 2.4 — Topic 2 Managed Identity | principalId `5c6021cc-e751-4d59-a9ae-96cbda7e3b00`; enabled via `az webapp identity assign` |
| Role: AcrPull → `webapp-estiam-dev-2` identity | RBAC role assignment | Section 2.4 — Topic 2 Managed Identity | Scope: `acrestiamdev2`. Same purpose as the Container App's AcrPull role, for the App Service side of the Module 1 gotcha |
| ACR admin user (`acrestiamdev2`) | Config change | Section 2.4 — Topic 2 Managed Identity | Disabled (`az acr update --admin-enabled false`) after both `$APP` and `$WEBAPP` were switched to identity-based pull — confirmed live that both still serve real data with the admin user off. The old `DOCKER_REGISTRY_SERVER_URL/USERNAME/PASSWORD` app settings on `$WEBAPP` were removed as part of this (superseded by `acrUseManagedIdentityCreds`) |

## Module 4

| Resource Name | Type | Created In (section/step) | Notes |
| -------------- | ---- | -------------------------- | ----- |
| `catalog-api:v7` (image) | Container image / App revision | Section 1.3 — Topic 1 Caching | Pushed to `acrestiamdev2`; deployed to `app-estiam-dev-2` as revision `app-estiam-dev-2--v7`, 100% traffic. New project folder duplicated from `catalog-api-v6` (unchanged, still live under its own history). Adds `GET /titles/count` — a real Cosmos DB `COUNT` aggregate cached in `IMemoryCache` for 30s; confirmed live MISS at 2.89 RU, HIT at 0 RU |
| `catalog-ui:v2` (image) | Container image / App Service deployment | Section 1.4 — Topic 1 Caching | Pushed to `acrestiamdev2`; deployed to `webapp-estiam-dev-2` via direct `az webapp config container set` (no deployment slot — `asp-estiam-dev-2` is B1, which doesn't support slots). Same `catalog-ui` project folder as Module 1 (grown in place, not re-versioned into a new folder). Adds a title-count pill (HIT/MISS + real RU) to the top bar, consuming `catalog-api-v7`'s `/titles/count` through the existing same-origin proxy |
| `law-estiam-appi-dev-2` | Log Analytics Workspace | Section 2 — Topic 2 Application Insights | westcentralus, PerGB2018 SKU. Dedicated workspace for Application Insights, deliberately separate from Module 1's auto-generated `workspace-rgestiamdev2NzD5` (the Container Apps environment's own platform-log workspace, left untouched — `az containerapp env update` has no option to repoint an environment's logs workspace after creation, and renaming an existing Azure resource isn't possible at all) |
| `appi-estiam-dev-2` | Application Insights component | Section 2 — Topic 2 Application Insights | **westus2**, not westcentralus — `microsoft.insights/components` isn't offered in westcentralus (confirmed live: `LocationNotAvailableForResourceType`). Workspace-based, linked to `law-estiam-appi-dev-2` above; the component and its workspace don't need to share a region |
| `catalog-api:v8` (image) | Container image / App revision | Section 2.3 — Topic 2 Application Insights | Pushed to `acrestiamdev2`; deployed to `app-estiam-dev-2` as revision `app-estiam-dev-2--v8`, 100% traffic. Adds `Azure.Monitor.OpenTelemetry.AspNetCore` (Requests/Dependencies/Exceptions/Traces), the Cosmos DB distributed-tracing fix (`EnableActivitySource` + `DisableDistributedTracing = false`), and a `?fail=true` demo hook on `/titles/count`. Also carries a `DefaultAzureCredentialOptions.ExcludeManagedIdentityCredential` fix for a local-dev-only credential regression caused by this package's transitive `Azure.Core` bump (see PRD gotchas) — confirmed live: real Cosmos dependency traces and the demo exception both arrived in Application Insights correctly |
| `catalog-ui:v3` (image) | Container image / App Service deployment | Section 2.5 — Topic 2 Application Insights | Pushed to `acrestiamdev2`; deployed to `webapp-estiam-dev-2` via direct `az webapp config container set` (no slot, same as `v2`). Same `catalog-ui` project folder, grown in place. Adds `Azure.Monitor.OpenTelemetry.AspNetCore` (one line, `UseAzureMonitor()`) — confirmed live that its outbound `HttpClient` call to `catalog-api` shows up as a correlated Dependency, same Operation Id as `catalog-api`'s own Request, `cloud_RoleName` auto-populated as `webapp-estiam-dev-2` with no manual config |

## Module 5

| Resource Name | Type | Created In (section/step) | Notes |
| -------------- | ---- | -------------------------- | ----- |
| `apim-estiam-dev-2` | API Management instance | Section 1 — Topic 1 API Management | Consumption SKU, westcentralus, in shared `rg-estiam-dev-2`. Gateway URL: `https://apim-estiam-dev-2.azure-api.net`. No dedicated IP address (inbound or outbound) on this tier — see manual's Issues & Fixes for the live investigation into why this rules out IP-based backend lock-down |
| `catalog-api` (APIM API) | APIM API definition | Section 1 — Topic 1 API Management | Imported from `catalog-api`'s live `/swagger/v1/swagger.json` (Container App `app-estiam-dev-2`); path `catalog`; `--service-url` set explicitly to the Container App FQDN (the swagger doc carries no `servers` entry, so APIM has no backend without it). Policies: `rate-limit` (3 calls / 30s, API-level, inbound) and `set-header X-Gateway` (outbound) |
| `poster-jobs` (queue) | Storage Queue | Section 3.2 — Topic 3 The Build | In `stestiamdev2`. Also the Function App's own `AzureWebJobsStorage` account, so binding is zero extra config |
| `func-estiam-dev-2` | Function App | Section 3.2 — Topic 3 The Build | Consumption plan, `dotnet-isolated`, .NET 8, Linux. Compute hosted in `eastus`, not `westcentralus` like the rest of this course's resources — forced by a live-found West Central US host-startup issue on this subscription, 2026-08-30 (see manual Issues & Fixes, Topic 3). Storage account (`AzureWebJobsStorage`) stays `stestiamdev2` in `westcentralus`, unchanged |
| `NormalizePoster` (Function) | Azure Function, `QueueTrigger` | Section 3.3 — Topic 3 The Build | `poster-normalizer` project. Triggers on `poster-jobs`, resizes the corresponding blob in `posters` to a 2:3 aspect ratio with SixLabors.ImageSharp (pinned to `2.1.9` — see Issues & Fixes), overwrites it, sets `normalized=true` metadata as an idempotency guard |
| `poster-created-sub` | Event Grid event subscription | Section 3.6 — Topic 3 The Build | Scoped directly to `stestiamdev2` (implicit system topic, no separate topic resource). Filters to `Microsoft.Storage.BlobCreated`, subject begins with `/blobServices/default/containers/posters/`. Endpoint: the `poster-jobs` Storage Queue |

## Module 6

| Resource Name | Type | Created In (section/step) | Notes |
| -------------- | ---- | -------------------------- | ----- |
| `aoai-estiam-dev-2` | Azure OpenAI account | Trainer setup — `commands.md` §1 | **`swedencentral`, not `westcentralus`** — Azure OpenAI is not offered in this course's default region, so `$AOAILOCATION` is its own variable (same pattern as `$FUNCLOCATION` in Module 5). S0 tier: no fixed cost, billed per token. Created with `--custom-domain`, which is what produces the `https://aoai-estiam-dev-2.openai.azure.com` endpoint form the API expects. Globally unique name. Requires the `Microsoft.CognitiveServices` provider, not registered by default |
| `gpt-5-mini` (deployment) | Azure OpenAI model deployment | Trainer setup — `commands.md` §1 | Model `gpt-5-mini` version `2025-08-07`, SKU `GlobalStandard`, capacity 100 (= 100K tokens/min, 100 requests/min). `area: EUR`. **`versionUpgradeOption: OnceNewDefaultVersionAvailable`** — Azure may move this to a newer model version unprompted, so re-run the behaviour probes before class. Chosen after `gpt-4o-mini` proved undeployable (`ServiceModelDeprecating`) and `gpt-5.4-mini` had a GlobalStandard quota limit of zero — quota is granted per model **and** per SKU |
| `azure-openai-key` (secret) | Key Vault secret | Trainer setup — `commands.md` §2 | In `kv-estiam-dev-2`. The only place the API key exists. Writing it needs `Key Vault Secrets Officer` on your own account — subscription Owner is not enough (RBAC-model vault, same as Module 3) |
| `Key Vault Secrets User` on `kv-estiam-dev-2` | Role assignment | Trainer setup — `commands.md` §2 | Granted to `app-estiam-dev-2`'s existing system-assigned managed identity. Data-plane access is a separate grant from ARM ownership — the same control-plane/data-plane split as Storage in Module 5 |
| `catalog-api:v9` (image) | Container image / Container App revision | Trainer setup — `commands.md` §3 | Pushed to `acrestiamdev2`; revision `app-estiam-dev-2--v9`. Adds `POST /assistant` (see `catalog-api-v9/AssistantEndpoint.cs`) — plain `HttpClient` to Azure OpenAI, no OpenAI SDK. Env vars set with `--set-env-vars` (never `--replace-env-vars`, which would wipe the Cosmos/Blob settings). The key arrives as a Container App secret defined as `keyvaultref:...,identityref:system` — a reference resolved at runtime, not a copy of the value. New revision landed at 0% traffic (multiple-revision mode) and needed an explicit `az containerapp ingress traffic set` |
| `AskAssistant` | APIM API operation | Trainer setup — `commands.md` §4 | `POST /assistant` on the existing `catalog-api` APIM API. **Added with `az apim api operation create`, deliberately NOT by re-importing the Swagger** — a re-import would have risked the `rate-limit` and `set-header` policies applied through the portal in Module 5. Without this the deployed UI gets `{"statusCode":404,"message":"Resource not found"}` — an APIM error shape, not ASP.NET's: the gateway does not learn new backend endpoints on its own |
| `catalog-ui:v4` (image) | Container image / App Service deployment | Trainer setup — `commands.md` §5 | Pushed to `acrestiamdev2`; deployed to `webapp-estiam-dev-2`. Same `catalog-ui` project folder as Modules 1 and 4, grown in place — adds the Assistant drawer (`wwwroot/js/assistant.js`, system-prompt textarea, grounding and memory switches, token readout) and exactly one new proxy method. All student state lives in `localStorage`, which is what lets ~50 students share this one deployment |
